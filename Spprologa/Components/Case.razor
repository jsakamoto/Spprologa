@inherits SpprologaComponentBase

@foreach (var sln in query(this.Query ?? "fail."))
{
    @ChildContent!(sln)
}
@code {
    [Parameter]
    public RenderFragment<Solution>? ChildContent { get; set; }

    [Parameter]
    public string? Query { get; set; }
}